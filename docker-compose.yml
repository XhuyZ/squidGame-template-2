# ok test1
version: '3.8'
services:
  server:
    image: squid-game-server
    build:
      context: ./server
    ports:
      - "4000:4000"

  client:
    image: squid-game-client
    build:
      context: ./client
    ports:
      - "3000:3000"
    depends_on:
      - server
